// Fill these in from Firebase console → Project settings → Your apps → Web app config.
// These values are meant to be public (client-side identifiers, not secrets) — real
// access control lives in Firestore Security Rules (see firestore.rules) checked
// against the admin-managed `members` collection, not a static list in this file.
export const firebaseConfig = {
  apiKey: 'REPLACE_ME',
  authDomain: 'REPLACE_ME.firebaseapp.com',
  projectId: 'REPLACE_ME',
  storageBucket: 'REPLACE_ME.appspot.com',
  messagingSenderId: 'REPLACE_ME',
  appId: 'REPLACE_ME',
};
